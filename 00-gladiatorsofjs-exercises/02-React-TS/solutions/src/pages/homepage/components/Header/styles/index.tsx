import styled from 'styled-components';

export const HeaderContainer = styled.header`
    background: var(--home_primary);
    display: flex;
    justify-content: center;
    align-items: center;
`;

export const HeaderTitle = styled.h1`
    color: var(--text_white);
    text-align: center;
    z-index: 10;
`;